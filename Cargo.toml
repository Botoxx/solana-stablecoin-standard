[workspace]
members = [
    "programs/sss-token",
    "programs/transfer-hook",
]
exclude = ["tui"]
resolver = "2"

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1
opt-level = "z"
